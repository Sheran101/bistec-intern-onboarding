You are implementing a Receipt Categoriser feature.

Use the spec and acceptance criteria only.

Do not invent:
- new response fields
- new categories
- new endpoints
- new behaviours

Required:
- separate LLM categoriser
- separate rule-based categoriser
- fallback when LLM returns 503
- tests for at least 3 acceptance criteria